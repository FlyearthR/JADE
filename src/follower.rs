//use crate::Message;

/*impl From<Vec<u8>> for Buffer {
    fn from(value: Vec<u8>) -> Self {
        let ptr = value.as_slice().as_ptr() as *const _;
        let len = value.len();
        Buffer { buffer: ptr, len: len as u64 }
    }
}*/

